apiVersion: v1
kind: ConfigMap
metadata:
  name: calendar-service-config
  labels:
    app.kubernetes.io/name: calendar
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: corporate-calendar
    app.kubernetes.io/instance: dev
    app.kubernetes.io/managed-by: kubectl
data:
  NODE_ENV: development
  AUTH_STRATEGY: azure
  AUTH_COOKIE_DOMAIN: ${AUTH_COOKIE_DOMAIN}
  POST_LOGIN_REDIRECT_URL: ${POST_LOGIN_REDIRECT_URL}
  CORS_ALLOWED_ORIGINS: 'https://calendar-ui-d8b00f-dev.apps.silver.devops.gov.bc.ca,https://calendar-ui-b3237c-dev.apps.gold.devops.gov.bc.ca,http://localhost:3000,http://localhost:4173,http://localhost:8080'
