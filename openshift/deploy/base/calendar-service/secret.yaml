apiVersion: v1
kind: Secret
metadata:
  name: calendar-service-secrets
  labels:
    app.kubernetes.io/name: calendar
    app.kubernetes.io/component: service
    app.kubernetes.io/part-of: corporate-calendar
    app.kubernetes.io/instance: dev
    app.kubernetes.io/managed-by: kubectl
type: Opaque
stringData:
  DATABASE_URL: postgres://postgres:calendarpwd@calendar-postgres:5432/calendar
  JWT_SECRET: ${JWT_SECRET} #should be set as a GitHub Actions secret before deploying. Example value: a long random string generated with a tool like openssl rand -hex 32
  AZURE_TENANT_ID: ${AZURE_TENANT_ID}
  AZURE_CLIENT_ID: ${AZURE_CLIENT_ID}
  AZURE_CLIENT_SECRET: ${AZURE_CLIENT_SECRET}
  AZURE_REDIRECT_URI: ${AZURE_REDIRECT_URI}
