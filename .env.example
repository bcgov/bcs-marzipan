# Database Configuration
# PostgreSQL connection string (required)
DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Database Connection Pool Settings (optional)
DB_MAX_CONNECTIONS=10
DB_IDLE_TIMEOUT=20
DB_CONNECT_TIMEOUT=10

# API Configuration
# API authentication key (optional in development, required in production)
# If not set, all requests are allowed in development mode
API_KEY=your-api-key-here

# Service Port
# Backend service port (optional, defaults to 3001)
PORT=3001

# Frontend Configuration
# Frontend API base URL (optional, defaults to http://localhost:3001)
# Used by Vite during build and runtime
VITE_API_BASE_URL=http://localhost:3001

CANADA_POST_API_KEY=your-canada-post-api-key-here

# Authentication Configuration
# Auth strategy: 'mock' for development, 'azure' for Azure AD OIDC (production)
AUTH_STRATEGY=mock

# Azure AD OIDC Configuration (required when AUTH_STRATEGY=azure)
# Obtain these values from your Azure App Registration in the BC Gov tenant.
AZURE_TENANT_ID=your-tenant-id
AZURE_CLIENT_ID=your-app-client-id
AZURE_CLIENT_SECRET=your-app-client-secret

# Optional: explicit OIDC callback URL override.
# Must exactly match a redirect URI registered in the Azure App Registration.
# If left blank the callback URL is auto-derived from the incoming request host,
# which works for most deployments without any change.
# Example: http://localhost:3001/api/auth/azure/callback
AZURE_REDIRECT_URI=

# JWT Configuration
# Secret key for signing JWT tokens (required in production, defaults to dev secret)
# IMPORTANT: Use a strong, random secret in production (min 32 characters)
JWT_SECRET=your-jwt-secret-change-in-production

# JWT token expiration time in seconds (optional, defaults to 3600 = 1 hour)
JWT_EXPIRES_IN=3600

SESSION_SECRET=your-session-secret-change-in-production
