/** Grid A bulk-select column: narrow width, no horizontal cell padding. */
export const GRID_A_SELECT_COLUMN_CELL_CLASS = 'overflow-visible px-0';

/** Header: centers the 16×16 checkbox; chevron sits outside (absolute). */
export const GRID_A_SELECT_CHECKBOX_HEADER_ALIGN_CLASS =
  'flex w-full items-center justify-center';

/**
 * Body row: top-aligned with other columns; 24px slot matches overview ID/actions row.
 */
export const GRID_A_SELECT_CHECKBOX_ROW_ALIGN_CLASS =
  'flex w-full items-start justify-center';
