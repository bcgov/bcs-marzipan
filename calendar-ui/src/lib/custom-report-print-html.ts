/**
 * Custom report static HTML (print/PDF / preview). Implemented in `@corpcal/shared` so the
 * calendar-service PDF path and the UI share one layout (mirrors {@link ReportTable} / {@link ReportRow}).
 */
export { buildCustomReportHtml } from '@corpcal/shared/reports/reportPrintHtml';
